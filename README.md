# Work-Search-Tracker
A Node.js utility that connects to Gmail via the Gmail API, fetches recent incoming emails, filters them by job search-related keywords, and displays relevant messages with details like subject, sender, and date on a web page.
